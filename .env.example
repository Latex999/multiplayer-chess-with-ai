PORT=5000
NODE_ENV=development
# MongoDB URI (optional) - if not provided, app will run without database
MONGODB_URI=mongodb://localhost:27017/chess-app
# JWT Secret for authentication
JWT_SECRET=your_secret_here
# Stockfish engine path (optional) - if provided, will use local Stockfish instead of WASM
STOCKFISH_PATH=